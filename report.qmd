---
title: "Spatial Justice through Urban Green Space Connectivity"
bibliography: references.bib
subtitle: "Project report of Group E"
author:
  - name: "Roxanne Vuijk"
    affiliation: "MSc Urbanism"
  - name: "Belina Aileen"
    affiliation: "MSc Geomatics"
  - name: "Daman Dogra"
    affiliation: "MSc Geomatics"
format:
  html:
    output-file: index.html
    theme: [cosmo, report.scss]
    toc: true
    number-sections: true
    code-fold: true
    embed-resources: true
---

```{r setup, include=FALSE}
source(here::here("R/00_config.R"))
```

<!-- # Introduction -->
## Introduction
Due to rapid urbanisation, the pressure on urban green space (UGS) increases and the benefits are often unevenly distributed across neighborhoods. Green spaces are essential urban infrastructure that influence the physical and mental well-being of inhabitants. They provide a wide range of services, such as urban cooling, air quality improvement and pose opportunities for social interaction. Not only are they beneficial to humans, they also support biodiversity conservation and species movement, thereby enhancing health, ecological, and social outcomes. 

However, not all citizens have equal access to these green spaces and they differ in quality. Therefore, their benefits depend not only on the presence of green spaces, but also on their accessibility, ecological quality, and spatial distribution. Research emphasizes implementing multi-functional blue-green infrastructure rather than isolated parks in contemporary planning processes. (Cardinali, 2024; Morris, 2003; Kasim, et al., 2019)

Connectivity is a key dimension of green space quality, since urban green spaces often function better as networks rather than independent elements. Connected green networks improve ecological functioning by facilitating movement of species and strengthens the ecosystem resilience. The amount of biodiversity and the ecological condition become indicators of urban green space quality. In addition, better connected green networks improve human access and facilitate movement through cities. Despite growing attention to green infrastructure networks, accessibility, ecological quality, and connectivity are still often assessed independently. (Irvine et al., 2008)

As defined by Roberto Rocco (n.d.), spatial justice concerns the equitable distribution of urban resources and opportunities across different population groups. When applied to urban green spaces, it considers not only the quantity of green space, but also its quality. Together they determine whether residents have equitable access to connected and ecologically functional green environments. This is important because unequal access can reinforce existing social and environmental inequalities, making spatial justice an increasingly important consideration in urban planning. (Logan et al 2019; De Sousa Silva et al., 2018)

Thus in this study, urban green space quality is understood as the combined performance of four dimensions: accessibility, ecological quality, spatial justice, and connectivity. Evaluating these dimensions together, they provide a more comprehensive assessment than considering each independently. To support evidence-based planning, this study integrates these dimensions into a reproducible analytical framework using a Spatial Multi-Criteria Decision Analysis (S-MCDA). The framework combines ecological, spatial, and social indicators to identify priority areas for intervention and is applied to two contrasting urban contexts: Yuexiu District, Guangzhou (China) and Delft (The Netherlands). The methodology is designed to be reproducible for both small cities and city districts. By using globally available datasets and a consistent analytical workflow, the framework can be transferred to other urban contexts to support nature-based planning strategies that improve urban green space quality and spatial justice. 

### Study Sites

In China, successful efforts have been made to implement green corridor networks, such as the Chengdu's Tiangfu Greenway, the Beijing urban green corridor, and the Pearl River Delta Greenway Network initiative. From 1986 to 2018, the built-up area of Guangzhou in China continued to increase. As a result of heavy urbanisation, the vegetation area continued to decrease during these 32 years (Guo et al, 2021). Guangzhou's urban greenway network was introduced as part of the wider Pearl River Delta Greenway Network initiative, which began in 2010. With this Guangzhou became one of the leading cities in implementing this strategy. A study by Tang et al., (2022) found that Guangzhou's greenways have met residents' needs between greenway supply and demand. However, high-density areas still prove to have unequal access to ugs. 

In contrast, Delft has developed within the Dutch planning tradition of integrating green and blue infrastructure into compact urban development. Rather than relying on large metropolitan greenway programmes, such as Guangzhou, Delft's urban green space has evolved through integrated spatial planning at a much smaller scale. The municipality actively integrates ugs through structural municipal policies and innovative pilot projects. Implementation of small-scale urban infrastructure and connections between neighbourhoods are considered important components of urban liveability. This planning approach makes Delft an interesting counterpart to Guangzhou. (Poptahof, Delft, The Netherlands | Urban Green-blue Grids, n.d.)

The study compares the urban contexts of Guangzhou, in China and Delft, in The Netherlands. Both cities have a historic urban core and have a relatively high urban green coverage. Urban green spaces cover about 34% of Guangzhou, and similarly, Delft is covered by 33% (HUGSI). Despite these similarities, comparing both cities presents a challenge because of their differences in scale and population density. Delft covers approximately 24 km2 and has a relative low density; the population is 102.000, while Guangzhou has a population of 18.680.000 over 7.433,3 km2 (WorldPop). As a result, Guangzhou is not only about 300 times as big on surface area, it is also approximately 6 times more densely populated than Delft. A direct comparison between the two cities would therefore be inappropriate, requiring the selection of a comparable spatial unit.

```{r}
#| label: fig-context
#| fig-cap: "Study sites of Guangzhou and Delft"
#| out-width: "100%"

knitr::include_graphics(file.path(OUT_ROOT,"fig_context_gz_dl.png"))
```

Guangzhou is administratively divided into districts. To get a comparable study area, this research focuses on Yuexiu District, the historical core of Guangzhou. Yuexiu covers 33.8 km2 and reports 1.173.100 inhabitants (WorldPop). Although still considerably denser than Delft, it represents a much more comparable spatial scale than the municipality of Guangzhou as a whole. The analytical framework can be applied consistently across both study areas with the use of this spatial unit. 

```{r}
#| label: fig-context
#| fig-cap: "Study sites of Yuexiu District (Guangzhou) and Delft."
#| out-width: "100%"

knitr::include_graphics(file.path(OUT_ROOT, "report_files/fig_context_yx_dl.png"))
```

## Research Questions

### Research questions

The main research question of this study is: 
opt 1. How does connectivity in between urban green spaces contribute to spatial justice in the cities Delft and Guangzhou? 

opt 2. How can accessibility, ecological quality, spatial justice, and connectivity be integrated into a spatial analytical framework to evaluate urban green space quality in Delft and Yuexiu?

opt 3. How does the connectivity of urban green spaces contribute to urban green space quality and spatial justice in Delft and Yuexiu District?

To answer this question, urban green space quality is evaluated through four complementary dimensions: accessibility, ecological quality, spatial justice, and connectivity. Each dimension forms a research objective with a corresponding sub-research question. The results of these analyses are integrated in a framework through a S-MCDA to indentify priority areas for interventions. 

### Research objectives

#### Objective 1: Accessibility

Assess the accessibility of urban green spaces for residents. 

::: {.callout-note appearance="simple"}
**SQ1:** How accessible are urban green spaces for residents?
:::

#### Objective 2: Ecological Quality

Assess the ecological quality of urban green spaces through biodiversity and typology analysis.

::: {.callout-note appearance="simple"}
**SQ2:** What urban green space typologies can be identified, and what is their ecological quality?
:::

#### Objective 3: Spatial Justice

Assess the spatial equity of urban green space distribution across administrative units. 

::: {.callout-note appearance="simple"}
**SQ3:** How equitable are urban green spaces distributed across administrative units?
:::

#### Objective 4: Connectivity

Assess the connectivity between urban green spaces as ecological networks. 

::: {.callout-note appearance="simple"}
**SQ4:** How well are urban green spaces connected to each other?
:::

#### Objective 5: Strategic Translation

Translate the analytical findings into nature-based planning strategies for improving urban green space quality.

::: {.callout-note appearance="simple"}
**SQ5:** Which areas require priority intervention, and what nature-based solutions can improve urban green space quality?
:::

## Methods

### Study Area

### Research Design
The study addresses the main research question through a comparative analytical framwoerk strcuture around the four thematic objectives:
1. Accessibility
2. Ecological Quality
3. Spatial Justice
4. Connectivity

Each objective corresponds to a sub research question and is operationalised through a set of spatial analysis methods applied consistently to both cities. The four objectuve scores are subsequently integrtaed through a Multi-Criteria Decision Analysis (MCDA) top produc a composite urban green space quality score and a spatial prioritisation of Nture-based Solution (NbS) intervention zones.

The study was conducted in two stages:
1. Stage I: All required spatial datasets were collected from public data sources, preprocesses into a consistent format, and validated. The resulting processes layers are provided in the project data folder abd form the fixed input to all subsequent analysis.
2. Stage II: All tehmatics analyses were conducted in R from those processed files. The analytical workflow described in Section 3.5 through 3.9 is fully reproducible form the provided data using the accompannying R scripts.

### Data Collection and Preprocessing
All spatial data were collected via a seven-step Python pipeline:

#### Administrative Boundaries
**Delft**

- Municipality boundary fetched from PDOK WFS (Kadaster bestuurlijke gebieden), filtered to municipality code `GM0503` and dissolved to a single polygon
- Available WFS layers auto-discovered via `GetCapabilities`; multiple BBOX orientations tried to handle GeoServer axis-order quirks; CBS gebiedsindelingen WFS (years 2024–2022) used as fallback
- CBS *buurten* (neighbourhood polygons) fetched from PDOK wijkenbuurten WFS, clipped to the municipality boundary, and filtered to buurt-level rows (`bu_code` prefix `BU`) to exclude wijk- and gemeente-level aggregates

**Yuexiu (Guangzhou)**

- District boundary and *jiēdào* subdistrict polygons obtained from GADM v4.1, filtered to `NAME_2 = "Guangzhou"` and dissolved to a single city polygon
- GADM GeoPackage (~200 MB) downloaded automatically if not present locally


#### Vector Layers (Green Space, Water, Roads)

**Delft — green space and water (BGT)**

- Green space fetched from the BGT (Basisregistratie Grootschalige Topografie) via the PDOK OGC API Features endpoint, with automatic pagination via `next` links
- Two feature types collected: `groenVoorziening` (maintained green infrastructure) and `bos` (forest and woodland)
- Water bodies fetched as BGT `waterdeel`, covering canals (*grachten*), rivers, ponds, and drainage channels
- BGT preferred over OSM because Dutch OSM data is derived from BGT via a lossy conversion; BGT carries 20 cm positional accuracy and is the legally authoritative source
- OSM used as fallback for both layers if BGT returned no features

**Yuexiu — green space, water, and roads (OSM)**

- All vector layers fetched from OpenStreetMap via the Overpass API
- Green space queried using tags `leisure=`, `landuse=`, and `natural=`
- Water bodies queried using `waterway=` and `natural=water`
- Requests retried automatically with exponential backoff on HTTP 429 (rate limit)

**Roads — both cities (OSM)**

- Road network fetched from OSM Overpass API using the `highway=` tag for both cities
- Bug fix applied: OSM ways with `highway=` that form a closed loop (roundabouts, dead-end loops) are stored as `LineString` regardless of node topology. Without this fix, closed ways are parsed as filled `Polygon` geometries, corrupting network topology and rendering as filled blobs in spatial analysis
- Zero-distance buffer applied to polygon geometries only to repair self-intersections; explicitly not applied to linestrings to preserve road network topology

**Retained tag attributes:** `leisure`, `landuse`, `natural`, `waterway`, `highway`, `name`, `surface`


#### Raster Layers

**WorldPop — both cities**

- Population raster downloaded at 100 m spatial resolution from worldpop.org
- Fetched via COG (Cloud-Optimised GeoTIFF) HTTP range requests using GDAL `/vsicurl/`, retrieving only the AOI window rather than the full national file
- Fallback to full national file download if COG range request failed

**Sentinel-2 NDVI — both cities**

- Cloud-free median NDVI composite generated via Google Earth Engine (GEE)
- S2 Surface Reflectance collection filtered by AOI, date range, and cloud percentage threshold
- NDVI computed per image as $(B8 - B4) / (B8 + B4)$ and reduced to a pixel-wise median
- Exported at 10 m native Sentinel-2 resolution
- Fallback to Copernicus STAC (`pystac-client`) if GEE was unavailable

**ESA WorldCover — both cities**

- Land cover raster downloaded at 10 m resolution from AWS S3 COG tiles
- Tiles covering the AOI stitched and clipped to city boundary

**VIIRS VNP46A4 — Yuexiu only**

- Annual mean nighttime light radiance composite downloaded from NASA Earthdata via `earthaccess`
- Used as socioeconomic proxy at subdistrict level; direct income data not publicly available for Yuexiu

**Yuexiu green space mask (derived)**

- Green space raster derived from WorldCover by retaining pixels classified as tree cover (class 10) or grassland (class 30)
- Further filtered by NDVI > 0.3 to exclude bare or paved surfaces misclassified as vegetation
- Binary GeoTIFF (10 m, 1 = green) saved and polygonised to GeoPackage
- Fragments < 100 m² removed; adjacent pixels dissolved then exploded to single-part polygons


#### Biodiversity Occurrences (GBIF)

- Species occurrence records fetched from the GBIF REST API (`occurrence/search` endpoint) for each city bounding box
- Records fetched in pages of 300, up to a maximum of 5,000 per city
- Filters applied:
  - `basisOfRecord = HUMAN_OBSERVATION`
  - Valid decimal coordinates required
  - `coordinateUncertaintyInMeters ≤ 1,000 m` (records with null uncertainty retained)
- Retained attributes: gbifID, species, genus, family, kingdom, latitude, longitude, coordinate uncertainty, year, month, basisOfRecord, datasetName
- Raw JSON cached locally; reruns load from cache without hitting the API


#### Socioeconomic Data

**Delft — CBS StatLine income**

- Neighbourhood-level income data fetched from CBS StatLine OData API, table `85163NED`
- Filtered to Delft buurt codes (`WijkenEnBuurten` starting with `BU0503`)
- Variables retrieved: mean disposable income per resident, mean income per earner, % low-income households, % near social minimum, % aged 65+, address density
- Joined to CBS buurt boundary polygons by neighbourhood code
- Two buurten with suppressed CBS disclosure excluded from income correlation analysis

**Yuexiu — VIIRS zonal statistics**

- Mean, median, and standard deviation of VIIRS nighttime light radiance computed per zone polygon from the raster downloaded in Step 3
- City-level AOI used as a single zone (subdistrict boundaries not freely available)
- Output saved as CSV (`guangzhou_viirs_zonal.csv`)

#### Clip and Reproject

All raw layers from Steps 1–5 clipped to the city AOI and reprojected to a metric CRS:

| City | CRS | Authority |
|---|---|---|
| Delft | EPSG:28992 — Amersfoort / RD New | Dutch national grid, metre units |
| Yuexiu | EPSG:4547 — CGCS2000 / Gauss-Krüger zone 20 | Chinese national grid, metre units |

Metric projection required for all buffer, area, and distance operations. Processing applied per layer type:

- **Vectors:** clipped with `geopandas.clip`; zero-distance buffer applied to polygons only to repair projection distortion artefacts; empty geometries removed
- **Rasters:** masked with `rasterio.mask`; reprojected with `rasterio.warp.reproject`; bilinear resampling used for continuous rasters (WorldPop, NDVI, VIIRS); nearest-neighbour resampling used for categorical rasters (WorldCover)
- All projected outputs saved with `_proj` suffix


#### Validation Audit

A validation audit was run on all projected output layers before analysis. Each layer was checked for:

- File existence and loadability
- Correct metric CRS matching the city target
- No null, empty, or invalid geometries
- Bounding box overlap with the city AOI
- Raster coverage > 50% valid (non-nodata) pixels
- Key attribute completeness ≥ 80% non-null (species and coordinates for GBIF; income for CBS)
- Cross-layer CRS consistency across all projected files for each city

Results were written to `validate_delft.json` and `validate_guangzhou.json`. Any layer failing a check was flagged before analysis proceeded.



### Analytical framework

This study addresses the research question by breaking it up in several research sub-questions, which will come together in forming a MCDA. The study hopes to provide a method to determine the quality of urban green spaces, and therefore provide a tool for planners and designers to identify aspects for urban green space improvement for more informed decisions.

The empirical analysis of this paper is split into four categories to provide more structure in throughout the data collection and evaluation, taking into account its multi-criterion nature. Within each of the categories, multiple relevant spatial analysis are conducted to provide meaning for the comparison between Delft and Yuexiu, to evaluate connectivity between green spaces.

-   **Accessibility** evaluates the population accessibility with green areas. The analysis conducted are; green space per capita, measuring the square meters of green space per person within an administrative boundary, buffer analysis to measure percentage of people within different walking distances (e.g. 300m and 500m), and mean nearest green space distance per sub-district, captured based on the centroid of each administrative boundary of the sub-district.

In a study by Meerow (2019) on green infrastructure, a spatial planning model for evaluating ecosystem service trade-offs and synergies was performed across three coastal mega-cities through a spatial criterion evaluation. One of the metrics was access to green space, which was used as an indicator representing the population weighted mean distance to the nearest park for all buildings within a census tract (Logan et al 2019), thus calculating distance between nearest park to building.

-   **Typology diversity** <!-- The spatial analysis conducted here include grouping by different green space according to its Open-Street Map data green area classification (e.g. forests, nature reserve, etc).  reveals the typology diversity but i think this is more to show distribution but not really for result analysis? -->

High levels of ecological connectivity within an urban landscape enables animals to move between patchy resources and allows for breeding whilst maintaining gene flow. In contrast, low levels of ecological connectivity restricts access to resources and prevents the movement of individuals and genes, inbreeding depression and extinction. One of the methodologies used in the study of Kirk et al. (2023) was quantifying ecological connectivity by a City biodiversity Index that calculates the probability of two randomly chosen locations to be connected and not separated by barriers.

    Species observation density is compared against patch size. The species observation density obtains its data from Global Biodiversity Information Facility (GBIF), an open-access database for biodiversity data. Thus, revealing the relationship between patch size (size of park) compared to the abundance of wildlife within it. $$\log(\text{Area}) \quad \text{vs.} \quad \log\left(\frac{\text{Observations}}{\text{Area}}\right)$$

    -   <!--  The violin plot takes mean NDVI (per sub-district) to be plotted against green typology. Thus, the plot reveals the variability and spread/distribution of NDVI value within each green space type. -> so we are evaluating whether each or which green space type has high or lower NDVI? it feels slightly redundant if we take a look how different green spaces if they have higher low NDVI? doesn't really help answer our question on biodiversity/accessibility? -->

    <!-- -->

    -   <!-- The green-blue ratio calculates the amount of area overlap between green features and water features, within each sub-district boundaries. Lower values indicate low water ratio compared to its amount of greeneries, whereas higher values reveal more water presence compared to the amount of green spaces.  discussed in gc, maybe we are trying to implement green-blue infrastructure (whatsapp gc) -->

<!-- addition, did we end up using k-means?-->

Urban green spaces are heterogeneous (source 2). Different spaces provide in different ecological and social functions. Typologies can help identify recurring patterns across neighbourhoods/contexts. To identify these recurring patterns, this research does a typology construction, making use of the indicators derived from the sub research questions. These observations are grouped based on indicator values. Through answering which urban green spaces are similar to others, there is a typology set created, which can then be used to classify and rate the chosen location on its urban green space quality through an MCDA. Resulting typologies reveal different combinations of accessibility, connectivity, biodiversity, and the neighbourhoods' demographic context.

-   **Spatial justice** Gini coefficient is a measure of statistical dispersion, used in this report too reveal the urban green space inequality (within meaningful residential population, current minimum population is 100). Value of 0 reflects perfect equality, whereas value of 1 represents maximum inequality. In this case, gini of 0 means equal access of green space for everyone, whereas 0, means that an area has nearly all the green spaces within the city. It is closely linked to the Lorenz curve as the Gini score is a direct reflection of the shape of the Lorenz Curve.

    Bivariate choropleth is also used to visualize density of green space per administrative boundaries against population. Lastly, green space-income correlation is used to capture the relationship between the two variables. <!-- i think it is better to use one data that can be synced with china and NL, cos now it is different, actual income data for NL but VIIRS (light) data for china -->

    *Lorenz curve* $$G = \frac{A}{A + B}$$

-   **Connectivity** The landscape structure uses number of patches (NP) and mean patch size (MPS) in hectares to define overall availability and average size of habitat fragments. Moreover Eucladian Nearest Neighbour (ENN) is also computed by taking the centroid of green patch, thus evaluating the distance of green patches from each other as metric of connectivity. Taking it further, the green patch centroids are used as network nodes. Edges are also defined as the line connecting these nodes. Through Breadth-First-Search (BFS), number of connected components are calculated. This also allows for isolated green patches to also be identified, where the gene flow across the city is blocked. This metric allow for analysis of both degree of centrality, measuring local connectivity of green patches, and also betweenness centrality, based on the edges connecting these centroids, revealing accessibility.

### MCDA

Urban green space quality cannot be defined by a single indicator and there are different dimensions of quality. With a Spatial Multi-Criteria Decision Analysis (MCDA) conditions in certain locations are evaluated through weighing different criteria. The categories derived from the research objectives are broken down to form these criteria and enables the integration of ecological, social, and spatial indicators into one framework. The MCDA framework combines indicators per category into an analytical dataset, where these indicators are weighted and aggregated into a quality score. The score allows spatial comparison between comparable spatial units; the Dutch wijken and the Chinese jiedao -- to be used as a decision-support tool for decision makers.

### Data collection

### Spatial unit

Spatial analysis outcomes depend on a chosen spatial unit. To ensure comparability, the study uses administrative neighbourhood units. However due to a scale difference, the comparison is done on scale of the entire city of Delft and the Yuexiu district in Guangzhou. These are comparable in surface (mention surface km2) and amount of neighbourhoods (mention #). The administrative units are wijken for Delft, and jiedao for Guanghzhou. Using administrative units increases policy relevance and reproducability through different scales (from neighbourhood to municipality) - difference in scale of Guangzhou and Delft, allows us to find comparable city districts and also allows planners/municipalities to work with the distinctive characters of city districts, makes it applicable in small cities as well as large cities.

## Results

### 1. Accessibility

```{r}
#| label: fig-access
#| fig-cap: "Access per capita map."
knitr::include_graphics(file.path(OUT_ROOT, "fig_access_per_capita.png"))
```

Delft neighbourhoods exhibit a mid-to-high, more evenly distributed green space-per-capita, while Yuexiu reveal low, more sparse green space composition within its core urban area. This can be attributed to the nature of scale of urbanization between the two cities where Yuexiu exhibit more megacity characteristic in comparison to Delft, exhibiting more medium-sized European city. With much higher population density of Yuexiu, 25,000/km² in comparison to Delft, 4,856/km² on population density, this discrepancy may explain the Yuexiu's need to prioritize more residential development over green spaces.

Moreover, the data reveals a large discrepancy in green space-per-capita ratio, with scale peaking to 400 m²/person in Delft, compared to merely 10 m²/person in Yuexiu. This is a 40 times fold difference in the upper scale bound. To make up for this large disparity, the visualization utilizes a uniform pseudo-log scale to compress the higher range while expanding the lower range, thus resulting in a more meaningful comparison between the two cities @fig-access.

```{r}
#| label: fig-meandist
#| fig-cap: "Mean Distance to the nearest green space."
knitr::include_graphics(file.path(OUT_ROOT, "fig_nearest_green_distance.png"))
```

```{r}
#| label: fig-buffer
#| fig-cap: "Buffer coverage percentage."
knitr::include_graphics(file.path(OUT_ROOT, "fig_buffer_coverage.png"))
```

The Mean distance to nearest green space, reveals the mean distance the population within each neighbourhood has to travel to reach a green space. Delft's scale tops at \~200m, even the worst-served wijk is close by Yuexiu standards. Yuexiu has one neighbourhood reaching 600m+ (the black patch in the northwest, same outlier as the blue-green ratio map, which had zero green space). The central strip of the high distance of neighbourhood to green space on the Delft map, corresponds to the rail corridor where green space is sparse. While both cities do not exhibit large disparity in accessibility to green space within each city, the between-city differences is more apparent evident in their maximum distance metrics @fig-meandist. This is also supported by the normalized walking distance to green space difference between the two cities on @fig-buffer.

```{r}
#| label: fig-network
#| fig-cap: "Access Points identification via Network Buffer Intersection"
knitr::include_graphics(file.path(OUT_ROOT, "fig_network_walk_access.png"))
```

Finally, the network analysis presented on @fig-network displays the access points of parks from road network, through intersecting a 300m green area buffer with road networks. Access points, visualized in orange, reveal a starking difference between the two cities. Within a 300m buffer, approximating to 3 to 4 minutes of walking, residents of Yuexiu historic district are able reach access points to green areas. In contrast to Yuexiu, Delft reveals a different spatial pattern. While fewer orange access points are mapped across Delft, its green spaces are distributed more evenly across the city. The widespread availability of green spaces is seen by its higher degree of buffer overlap, covering almost the entire city of Delft - that is different compared to Yuexiu, exhibiting more sparse distribution of urban green spaces. It can thus be concluded that while Yuexiu may exhibit more access point to parks, Delft may not exhibit as many access points, as most of the city, are already accessible to green areas, within 100-300-500m buffers (approximating to 1.5m, 3m, and 6.5m of walking distance).

### 2. Typology diversity & Biodiversity

As background context, @fig-green_typology reveal the the green space composition in urban area taken from the Open-Street Map data (e.g. forests, nature reserve, etc) between the two cities. While Delft seem to be dominated by green or meadows, Yuexiu green space composition is dominated by parls/recreation. The difference is consistent with the urban characteristic between the two cities; while Delft is considered a relatively small European city, Yuexiu historical city center pose more dense, urban characteristic, where presence of formal parks are more easily found compared more scarce grassland/meadow-type green spaces.

```{r}
#| label: fig-green_typology
#| fig-cap: "Spatial Composition of Urban Green Typologies"
knitr::include_graphics(file.path(OUT_ROOT, "fig_green_typology.png"))

```

```{r}
#| label: fig-ndvi-violin
#| fig-cap: "NDVI Violin"
knitr::include_graphics(file.path(OUT_ROOT, "fig_ndvi_violin.png"))
```

The NDVI violin plot can be interpreted as first, high consistency in the specific patch type to be very similar vegetation health scores; indicating good maintenance of urban green areas for narrow/pointy plots, and second, high variance in NDVI value across parks, suggesting possible differences in prioritisation of maintenance of different green areas. Based on these, three following interpretations can be made 1) Delft's forest/woodland patches cluster tightly at NDVI ≈ 0.75 (2) Yuexiu's parks have wider spread between 0.35–0.75, thus green quality is highly variable.​

```{r}
#| label: fig-ndvi-spatial
#| fig-cap: "NDVI Spatial Distribution and Zonal Means"
knitr::include_graphics(file.path(OUT_ROOT, "fig_ndvi_zonal.png"))
```

The Normalised Difference Vegetation Index (NDVI) is a remote sensing metric used to measure and quantify the health, density and greenness of vegetation. The figure @fig-ndvi-spatial, reveals the spatial distribution of NDVI relative within each cities' neighborhoods. Comparing the two cities, Delft's northwestern neighborhoods have relatively higher NDVI (high NDVI ≈ 0.7–0.8), yet lowers as it reached central areas of the city. In contrast, Yuexiu reveals a low to mid-mean NDVI level (NDVI ≈ 0.2–0.6), indicating sparse vegetation, with higher NDVI values observed around the northern neighborhoods. NDVI thus complements OSM's land use data as it measures actual health of vegetation, beyond its land-use mappings.

```{r}
#| label: fig-gbif
#| fig-cap: "GBIF density"
knitr::include_graphics(file.path(OUT_ROOT, "fig_gbif_density.png"))
```

The biodiversity observation density vs patch size suggest an interesting correlation; first, small patches are found to have significantly higher recorded observations per hectare, whereas larger patches are found to have much lower observations per hectare. This phenomenon can also be known as sampling artefact, where more observations are gathered in well-surveyed community sites as community gather near entrances, and not necessarily due to richer biodiversity. A second observation reveals the cluster of high patch area (\>10 ha) found in Yuexiu found to have low number of observations. While it can be attributed to the previous phenomenon, it may also indicate under sampling of the larger green spaces, thus, instead of reflecting low ecological biodiversity, the recorded data is more likely to under-represent their true biodiversity.

```{r}
#| label: fig-bluegreen
#| fig-cap: "Blue-green ratio"
knitr::include_graphics(file.path(OUT_ROOT, "fig_blue_green_ratio.png"))
```

Blue green balance, calculated by \$\\text{blue-green balance} = \\log(1 + \\text{water per capita}) - \\log(1 + \\text{green per capita})\$ reveal higher abundance of blue space (water) if dominated by positive values, higher abundance of green space if dominated by negative values, and relatively balanced green-blue space with values nearing 0. Comparing the two cities, the range in comparison for Yuexiu suggests much higher green space presence in comparison to water. This is validated by doing summary statistics between total water vs green, in which Yuexiu yielded 192,494m^2^ of mean green space compared to 40,304m^2^ of mean water space. In comparison, Delft yielded a more evenly spread balance between green and blue areas; akin to its canal-rich characteristic, thus reflecting its water-city heritage. The relevance extends as blue and green infratsructure together, can provide better cooling and biodiversity than either alone (@akther2026bgi). Limitation for now, for future work possible to relate the green-blue ratio with cooling/biodiversity and explore the relationship.

### 3. Spatial Justice

Compare how strong these relationships are in different cities (Delft vs Guangzhou). How does urban green space relate to spatial justice in Delft compared to Guangzhou? which city performs better on each analysis? which one is more equitable

gini coefficient green space per capita inequalities relationship between population density and green access income correlation what neighbourhoods are troubled? does this have to do with the heritage core in delft as well as guangzhou (both heritage centres) When a Gini-coefficient is higher, it means there is a more unequal distribution of urban green spaces. Accessibility and spatial justice vary across different neighbourhoods.

```{r}
#| label: fig-equity-correlations
#| fig-cap: "Equity correlations"

knitr::include_graphics(file.path(OUT_ROOT, "fig_equity_correlations.png"))
```

Studies (@unlit and @unlit2) have revealed that night-time light (VIIRS) can serve as a reliable proxy to examining economic development of certain regional area. To this finding, due to our inability to find the spatial distribution of income in Yuexiu sub-district, China, VIIRS night-light is used in this graph. As for the Netherlands, with data availability on income per neighborhood (Delft: CBS disposable income), the direct metric is used instead. The equity correlation displayed by @fig-equity-correlations, reveal both statistically insignificant finding of green spaces versus indicator of economic status. The correlation of income as direct measure of health is found to be -0.117 for Delft, and -0.122 for Yuexiu. Both exhibit negative, near-zero values, suggesting very little to no significant trend of green spaces favoring wealthier sub-districts or neighborhoods in both compared locations. Relative to income, its distribution is relatively random.

```{r, out.width="30%"}
#| label: fig_bivariate_legend.png
knitr::include_graphics(file.path(OUT_ROOT, "fig_bivariate_legend.png"))
```

```{r}
#| label: fig-bivariate-choropleth
#| fig-cap: "Bivariate Choropleth"

knitr::include_graphics(file.path(OUT_ROOT, "fig_bivariate_choropleth.png"))
```

The bivariate choropleth @fig-bivariate-choropleth displays the interaction between a demographic factor of population density and an environmental factor of green density. Between the two cities, Delft reveals a more balanced spatial transition; of steady balance between areas dominated in high green density with low population (green) and zones characterized by high population density with limited greenery (pink). Yuexiu does not display this characteristic; instead, it reveals less uniform transition of greenery-population density balance amongst its neighborhoods. This suggests that while Delft’s green-population dynamic is more structural, Yuexiu’s landscape is the result of uneven, rapid growth where increase in residential density may have outpaced the provision of ecological infrastructure in the city of Yuexiu.

```{r}
#| label: fig-gini-coeff
#| fig-cap: "Gini Coefficient comparison"

knitr::include_graphics(file.path(OUT_ROOT, "fig_lorenz_gini.png"))
```

Lastly, spatial justice is examined by comparing the behavior of Lorenz gini of the two cities @fig-gini-coeff. The dotted diagonal grey line represents perfect state of equality where green space would have been equally distributed among all its neighborhoods; 0, means perfect equality, whereas 1 represents perfect inequality. Delft (Gini coefficient= 0.605) reveal a more equal distribution of green spaces across the city compared to Yuexiu (Gini coefficient= 0.719). This can be attributed back to the spatial distribution shown on the map of Figure @fig-bivariate-choropleth, displaying how the inequality exists (while being relative to population density), or more simply Figure @fig-access on access to green space per neighborhood.

### 4. Connectivity

```{r}
#| label: fig-eucl
#| fig-cap: "Eucladian Nearest Neighbour Distance"
#| 
knitr::include_graphics(file.path(OUT_ROOT, "fig_enn_distribution.png"))
```

Through figure @fig-eucl we can see how majority of the green patches across the city are close to one another (below the 200m threshold). This indicate high connectivity (based on low distance traveled between one green patch to another). However this is not the case with Yuexiu, where green patches are located further away from each other (all exceeding the 200m threshold). This confirms that Yuexiu has more structurally isolated green patches with one another compared to Delft.

```{r}
#| label: fig-fragm
#| fig-cap: "Fragmentation metrics"
#| 
knitr::include_graphics(file.path(OUT_ROOT, "fig_fragmentation_metrics.png"))
```

Figure @fig-fragm reveal that while Delft has much higher number of patches, its distance between the patches is also subsequenly lower than of Yuexiu's. However, this high number of patches is also followed by its patch size that is not very large. This is the opposite when compared to Yuexiu, that while number of patches is much lower and distance in between patches is much further away compared to Delft, the size of each patch seen by the percentage of patches exceeding 1 ha and mean patch size (in ha) are much larger compared to Delft. This implies that while Delft focuses more on more well-connected green patches across the city, Yuexiu may focus more on providing large green spaces as leisure than that of connectivity, as demonstarted by Delft.

```{r}
#| label: fig-connectmaps
#| fig-cap: "Green patch connectivity maps for Yuexiu and Delft."
#| 
knitr::include_graphics(file.path(OUT_ROOT, "fig_connectivity_maps.png"))
```

The figure @fig-connectmaps visualizes UGS as a functional network, as it takes centroids out of each patch (as nodes) and edges, representing potential dispersal paths between each green patches. The edges then function as potential corridors to enable movement of wildlife or human between the otherwise green patches. Thus based on this, areas with least orange lines/edges represent where weakest links are due to its far proximities with other green areas. In contrast to Delft's high connectivity network, Yuexiu reveals more limited potential for connectivity through green corridors. This finding is consistent with previous figures' finding @fig-fragm where green pacthes in Yuexiu may be further away compared to delft, where green patches are closer to each other yet smaller. Despite the lack of potential corridors in Yuexiu, the city's larger green patches still offer potential for possible connectivity. However, this implies that more planning and infrastructure investment needs to exist for connectivity to be in place.

```{r}
#| label: fig-betweenness
knitr::include_graphics(file.path(OUT_ROOT, "fig_betweenness_vs_area.png"))
```

### 5. MCDA

```{r}
#| label: fig-mcdamap
#| fig-cap: "MCDA map"

knitr::include_graphics(file.path(OUT_ROOT, "fig_mcda_maps.png"))

```

@fig-mcdamap visualizes a normalized figure of the level of grene infrastyructure urgency needs to be implemented (0-1). Dark green suggests low urgency for the need of more intervention, while dark red suggesting high urgency. In Yuexiu, the spatial pattern is more fragmented due to its historical characteristic context. On the other hand, Delft's high uregncy zone is less fragmented and more concentrated in the central-north strip. This finding aligns with @fig-meandist, which reveal how isolated these neighborhoods are with other existing green patches. Moreover, assigning higher priority to these areas is justified as there needs spatial equity to be addressed, consequently fostering better connectivity in the broader urban network.

```{r}
#| label: fig-mcdaradar
#| fig-cap: "Suggestion for green corridors"
#|
knitr::include_graphics(file.path(OUT_ROOT, "fig_mcda_radar.png"))
```

For each of the four criteria, the city-wide mean of the normalized urgency scores (@fig-mcdamape) is calculated and displayed on @fig-mcdaradar. Thus, higher bar indicate greater average urgency compared to other dimensions. In both cities, connectivity pose higher relative urgency compared to other dimensions, opposing to equity/spatial justice, achieving lowest need for intervention. This is an interesting observation as connectivity is both central to the discussion of nature-based solution and efforts seemed to have been made to ensure connectivity of green patches in Delft, however, it still seems to need most urgent care compared to other metrics. The finding thus suggests the need for explicit connectivity effort through establishments of green corridor between green patches in both cities as part of further effort to our focus; nature-based solutions.

```{r}
#| label: fig-prioritytiers
#| fig-cap: "Green patch connectivity maps for Yuexiu and Delft."
#|
knitr::include_graphics(file.path(OUT_ROOT, "fig_priority_tiers.png"))
```

The priority tier (@fig-prioritytiers) divides the range of tiers equally to three groups (tertile classification). This is a choice of classification, which does not aim to exactly set 33% of a city to genuinely needs more urgent intervention as our choice of normalization also does not take into account the within group value difference to really determine the amount of urgent care actually required. Consistent with previous figures, Yuexiu's high priority zones are scattered, reflecting more fragmented planning scheme in terms of urban green infrastructure, compared to Delft where its high-priority zones are more concentrated, that may suggest a more underlying systematic issue around the area.

```{r}
#| label: fig-nbscorridors 
#| fig-cap: "Suggestion for green corridors" 
#| 
knitr::include_graphics(file.path(OUT_ROOT, "fig_nbs_corridors.png"))
```

@fig-nbscorridors overlays areas of high priorities determined by the MCDA and isolated green areas. The overlay of the two layers result in more easily identifiable places/locations, as direct correspondance to our nature-based solutions. As seen from the figure (@fig-nbscorridors), Yuexiu exhibit much more need for potential corridors compared to Delft. This finding is consistent as also with Yuexiu's fragmented green patches across the city; consequently, more interventions is needed to establish functional connectivity between these isolated sites.

## Discussion

Translate analytical findings into strategies for increasing UGS quality. How do the differences between the two cities show how connectivity and accessibility can improve spatial justice? What strategies are suggested to improve spatial justice? Potential interventions: green corridors, linear parks, canal-side ecological networks, pocket parks, urban forest, biodiversity stepping stones, street greening, green roofs and walls. - connect those to a weakness found in the analysis.

### Reproducibility

As sources are gathered through globally availbale datasets, methodology can be replicated in other cities/districts. Framework is also available on Github and independent of local planning systems. GIS workflow can be reproduced in QGIS and R indicators depending on local contexts.

### Limitations

Few limitations have been identified in accordance to our data source. First, most of our datasets come from OSM. Its data completeness however differ between Yuexiu, Guangzhou and Delft, the Netherlands as OSM's volunteer-based system is more dominant in Wester European cities compared to Chinese cities. This means that data gathered is not the most up-to-date nor accurate, or could be biased. Moreover, language barrier in accessing chinese dataset makes it more difficult to perform more direct comparison between the two cities, especially within the spatial justice aspect. Here, instead of using income per sub-district data, our methodology had to rely on VIIRS night-light data as proxy to economic development. While there have been literatures supporting the linkage between VIIRS night-light data as economic proxy for development, this still does not provide direct comparison between the two cities. Furthermore, GBIF observations from Typology diversity & Biodiversity section may contain sampling bias as Chinese dataset is much more up to date in comparison to Delft dataset. Interpretation becomes less meaningful due to this sampling bias. In obtaining the datasets, we are also aware of the temporal effect if data gathered were to be obtained throughout different seasons. For example, the comparison would not be less meaningful if NDVI obtained for Delft and Yuexiu were obtained during different seasons.

There are also few limitations identified within our code methodology. The MCDA weights were assigned and not derived @fig-connectmaps. This means that our choice to assign weight accessibility at 30%, biodiversity and connectivity at 25%, and equity/spatial justice at 20% is a judgement made before analysis. This however can differ to other researcher or planning priorities, as the weights assigned were based on judgement with nature-based solution being central to our focus analysis. Moreover, @fig-prioritytiers displays relative ranking, and does not display absolute urgency to where there needs immediate intervention to nature-based solution. It attempts to inform the least well-performing regions, rather than genuine crisis/urgency.

### Future Work

## Conclusion

Spatial justice is not only determined by the quality of green space, but by how well green spaces are connected, accessible, and ecologically functional.
